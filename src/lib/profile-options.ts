// Shared by onboarding and Settings so the option lists (and therefore the
// values written to profiles.overall_goal / profiles.position) never drift
// out of sync between the two places they're editable.
export const GOALS = [
  "Improve consistency",
  "Better net game",
  "Stronger serve",
  "Improve movement",
  "Win more matches",
  "Play more often",
];

export const POSITIONS = ["Left wall", "Right wall", "Both"];
